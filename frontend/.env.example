# Server-side target for same-origin /api rewrites.
API_PROXY_TARGET=http://localhost

# Leave empty for same-origin requests; set only when a separate public API origin is required.
NEXT_PUBLIC_API_URL=
